# @package _global_

defaults:
  - override /data: package_scan_v6
  - override /model: codewam
  - _self_

batch_size: 2
num_workers: 0

model:
  mot_checkpoint_mixed_attn: false

max_steps: 1
log_every: 1
save_every: 100
eval_every: 100
wandb:
  enabled: false
