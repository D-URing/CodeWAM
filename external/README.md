# External Code

Run:

```bash
bash scripts/bootstrap_fastwam.sh
```

to materialize the selected FastWAM subtree at `external/FastWAM`.

The checkout is intentionally ignored by git. Commit only CodeWAM-owned code,
configs, scripts, and docs.
